pipeline {
  agent any 
    stages {
    stage ( 'BUILD' ) {
                steps {
                 sh '''
                    cd /var/lib/jenkins/workspace/pipelinec-program
                    make ABC.exe
                    '''
                    }
                 }
             }
         }
