pipeline {
  agent any 
    stages {
    stage ( 'BUILD' ) {
                steps {
                 sh '''
                    cd /var/lib/jenkins/workspace/pipeline_jenkins
                    make ABC.exe
                    '''
                    }
                 }
             }
         }
